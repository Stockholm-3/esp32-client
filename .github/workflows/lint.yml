name: Lint

on:
  push:
    branches: ["**"]
    paths-ignore:
      - "**.md"
      - ".gitignore"
      - "LICENSE"
      - "docs/**"
  pull_request:
    branches: ["**"]
    paths-ignore:
      - "**.md"
      - ".gitignore"
      - "LICENSE"
      - "docs/**"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: clang-tidy
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Nix
        uses: cachix/install-nix-action@v25
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: Magic Nix Cache
        uses: DeterminateSystems/magic-nix-cache-action@v2

      - name: Cache Espressif Toolchains
        id: cache-espressif-tools
        uses: actions/cache@v4
        with:
          path: /home/runner/.espressif
          # Pin toolcache index to your flake's lock file to update if pins change
          key: espressif-tools-${{ hashFiles('flake.lock') }}

      - name: Prepare Compile Database
        run: |
          nix develop . --command idf.py reconfigure

      - name: Run Linter
        run: |
          # Pull standard host tools from your Nix flake shell environment map
          ESPRESSIF_CLANG_TIDY=$(nix develop . --command which clang-tidy)
          echo "Discovered target linter path inside Flake shell: $ESPRESSIF_CLANG_TIDY"
          
          # Force the Makefile runner to use the flake context binary
          nix develop . --command make lint-ci CLANG_TIDY_EXE="$ESPRESSIF_CLANG_TIDY"

      - name: Upload lint DB on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: clang-tidy-db-${{ github.sha }}
          path: build/lint/compile_commands.json
          retention-days: 7
